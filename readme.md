# bounce

Nothing much here.

## Notes

- Converting a .mov to .mp4: `ffmpeg -i input.mov -c:v libx264 -c:a aac -pix_fmt yuv420p output.mp4`

## TO DO

- detect if fast moving objects have teleported across a boundary
